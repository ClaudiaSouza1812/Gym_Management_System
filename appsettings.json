{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Database connection strings section
  "ConnectionStrings": {
    // Specific connection string for this application
    // Defines how to connect to the database
    "CA_RS11_P2-2_ClaudiaSouza_ConnectionString": "Server=localhost;Database=CA_RS11_P2-2_ClaudiaSouza;Trusted_Connection=True"
    // - Server: Specifies the database server (localhost in this case)
    // - Database: Names the specific database to connect to
    // - Trusted_Connection=True: Uses Windows Authentication for database access
  }
}
